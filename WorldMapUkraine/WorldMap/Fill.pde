class Fill {
  
  /*
    int based = (int)check.get(0).x + ((int)check.get(0).y*worldmap.width);
   worldmap.pixels[based] = primary;
   if (worldmap.pixels[based-1] == base) {
   worldmap.pixels[based-1] = primary;
   } else if (worldmap.pixels[based+1] == base) {
   worldmap.pixels[based+1] = primary;
   } else if (worldmap.pixels[based-width] == base) {
   worldmap.pixels[based-width-1] = primary;
   }
   
   for (int i=0;i<check.size();i++) {
   int ypos = (int)check.get(i).y;
   int xpos = (int)check.get(i).x;
   int based = (int)check.get(i).x + ((int)check.get(i).y*worldmap.width);
   worldmap.pixels[based] = primary;
   if (worldmap.pixels[based-1] == base) {
   worldmap.pixels[based-1] = primary;
   } else if (worldmap.pixels[based+1] == base) {
   worldmap.pixels[based+1] = primary;
   } else if (worldmap.pixels[based-width] == base) {
   worldmap.pixels[based-width-1] = primary;
   }
   }
   */
  
}
